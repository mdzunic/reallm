// Placeholder: SPEC-010 onward put the systems suites here (progression,
// economy, balance, combat, enemyAi, spatialHash, layout, spawn, weather,
// missions, flight — SPEC-016 §3). Vitest runs `tests/**/*.test.ts` only.
export const SUITE = 'systems' as const;
