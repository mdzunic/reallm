// Placeholder until SPEC-012 lands the entity → mesh views here. Views own the
// Three.js objects and read entity state; they never import `systems`
// (SPEC-001 §4).
export const FOLDER = 'views' as const;
