// Placeholder until SPEC-003 lands the `Scene` contract, the `SceneManager`
// and the concrete scenes here. Exists so the folder is present and the
// architecture tests (tests/architecture/) have a file to check.
export const FOLDER = 'scenes' as const;
